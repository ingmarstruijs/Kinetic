name: kinetic_link_bridge
description: Session + RPC protocol for Kinetic Link Web (phone bridge).
version: 0.1.0
resolution: workspace
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  crypto: ^3.0.6
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
